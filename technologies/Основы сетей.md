# Основы сетей

> Фундаментальные знания сетевых технологий

## 🎯 Почему это критически важно

Сети - это основа любой IT-инфраструктуры. Без понимания сетей невозможно эффективно администрировать серверы, диагностировать проблемы или проектировать инфраструктуру. Это один из самых важных навыков системного администратора.

## 📊 Уровни владения

### [[Junior SysAdmin]] уровень

#### Модель OSI и TCP/IP
**Модель OSI (7 уровней):**
1. Физический - кабели, сигналы
2. Канальный - MAC-адреса, коммутаторы
3. Сетевой - IP-адреса, маршрутизация
4. Транспортный - TCP/UDP, порты
5. Сеансовый - сессии
6. Представления - шифрование, сжатие
7. Прикладной - HTTP, FTP, DNS

**TCP/IP модель (4 уровня):**
1. Сетевой доступ
2. Интернет (IP)
3. Транспортный (TCP/UDP)
4. Прикладной

#### IP-адресация

**IPv4**
```
192.168.1.100/24
│   │   │ │   └─ маска подсети (/24 = 255.255.255.0)
│   │   │ └───── хост
│   │   └─────── подсеть
└───────────────  сеть
```

**Частные диапазоны:**
- 10.0.0.0/8 (10.0.0.0 - 10.255.255.255)
- 172.16.0.0/12 (172.16.0.0 - 172.31.255.255)
- 192.168.0.0/16 (192.168.0.0 - 192.168.255.255)

**Подсети:**
```
192.168.1.0/24    - сеть с 256 адресами (254 хоста)
192.168.1.0/25    - 2 подсети по 128 адресов
192.168.1.0/26    - 4 подсети по 64 адреса
192.168.1.0/27    - 8 подсетей по 32 адреса
```

**IPv6 (базовое понимание)**
```
2001:0db8:85a3:0000:0000:8a2e:0370:7334
```

#### Основные протоколы

**TCP (Transmission Control Protocol)**
- Надежный, с установлением соединения
- Three-way handshake (SYN, SYN-ACK, ACK)
- Гарантирует доставку и порядок
- Используется: HTTP, HTTPS, SSH, FTP

**UDP (User Datagram Protocol)**
- Ненадежный, без установления соединения
- Быстрый, без overhead
- Используется: DNS, DHCP, streaming, VoIP

**ICMP**
- Диагностика и сообщения об ошибках
- `ping` использует ICMP

**Порты**
- Well-known порты (0-1023):
  - 22 - SSH
  - 80 - HTTP
  - 443 - HTTPS
  - 53 - DNS
  - 3306 - MySQL
  - 5432 - PostgreSQL
- Registered (1024-49151)
- Dynamic/Private (49152-65535)

#### Команды диагностики

**ping** - проверка доступности
```bash
ping 8.8.8.8
ping google.com
```

**traceroute/tracert** - трассировка маршрута
```bash
traceroute google.com
```

**nslookup/dig** - DNS запросы
```bash
nslookup google.com
dig google.com
```

**netstat/ss** - сетевые соединения
```bash
netstat -tulpn  # Linux
ss -tulpn       # Современная замена netstat
```

**ip** - сетевая конфигурация (Linux)
```bash
ip addr show
ip route show
ip link show
```

**ifconfig** (устарела, но все еще используется)
```bash
ifconfig
```

#### [[NAT]] (Network Address Translation)
- Преобразование частных IP в публичные
- SNAT (Source NAT)
- DNAT (Destination NAT) - port forwarding
- Необходимо для понимания работы роутеров

#### DHCP и DNS
- [[DHCP]] - автоматическое получение IP
- [[DNS]] - преобразование имен в IP

### [[Middle SysAdmin]] уровень

#### VPN (Virtual Private Network)

**Типы VPN:**
- **Site-to-Site VPN** - соединение сетей
- **Remote Access VPN** - удаленный доступ пользователей

**Протоколы:**
- **OpenVPN** - open source, надежный
- **WireGuard** - современный, быстрый
- **IPSec** - enterprise стандарт
- **L2TP/IPSec** - комбинация протоколов

**Настройка OpenVPN (пример сервера):**
```bash
# Установка
apt install openvpn easy-rsa

# Генерация сертификатов
./easyrsa init-pki
./easyrsa build-ca
./easyrsa gen-req server nopass
./easyrsa sign-req server server
```

#### VLAN (Virtual LAN)
- Логическое разделение сети
- 802.1Q стандарт
- Trunk и access порты
- Inter-VLAN routing
- Связано с: [[Сетевое оборудование]]

#### [[Маршрутизация]]

**Статическая маршрутизация:**
```bash
# Linux
ip route add 10.0.2.0/24 via 10.0.1.1

# Постоянная маршрутизация
# /etc/netplan/ или /etc/network/interfaces
```

**Динамическая маршрутизация (базовое понимание):**
- RIP - устаревший
- OSPF - Link State протокол
- BGP - для интернет-маршрутизации

#### Firewalls
- **iptables/nftables** (Linux)
  - Связано с: [[Firewall и сетевая безопасность]]
  - Цепочки: INPUT, OUTPUT, FORWARD
  - Таблицы: filter, nat, mangle
- **firewalld** (RHEL/CentOS)
- **ufw** (Ubuntu - упрощенный frontend для iptables)

**Примеры iptables:**
```bash
# Разрешить SSH
iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Разрешить HTTP/HTTPS
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# Разрешить established соединения
iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# Отклонить все остальное
iptables -P INPUT DROP
```

#### Балансировка нагрузки
- Связано с: [[Load Balancing]]
- Layer 4 (Transport) балансировка
- Layer 7 (Application) балансировка
- Методы: Round Robin, Least Connections, IP Hash
- Health checks

#### Troubleshooting сложных проблем

**tcpdump** - анализ трафика
```bash
# Захват трафика на интерфейсе
tcpdump -i eth0

# Захват HTTP трафика
tcpdump -i eth0 port 80

# Сохранение в файл
tcpdump -i eth0 -w capture.pcap

# Фильтр по хосту
tcpdump -i eth0 host 192.168.1.100
```

**Wireshark** - GUI анализатор пакетов

**curl** - тестирование HTTP
```bash
curl -I https://example.com  # Только headers
curl -v https://example.com  # Verbose режим
curl -X POST -d "data" https://api.example.com
```

#### Сетевая безопасность (средний уровень)
- Port scanning (nmap)
- DDoS protection (базовое понимание)
- Rate limiting
- Fail2ban

### [[Senior SysAdmin]] уровень

#### BGP (Border Gateway Protocol)
- Протокол маршрутизации интернета
- AS (Autonomous Systems)
- Path selection
- BGP communities
- Необходимо для работы на уровне ISP или больших компаний

#### OSPF (Open Shortest Path First)
- Link State протокол
- Areas и backbone
- DR/BDR выборы
- Используется в enterprise сетях

#### SDN (Software Defined Networking)
- Отделение control plane от data plane
- OpenFlow протокол
- Network programmability
- Примеры: Open vSwitch, Cisco ACI

#### Advanced troubleshooting

**mtr** - комбинация ping и traceroute
```bash
mtr google.com
```

**iperf** - тестирование пропускной способности
```bash
# На сервере
iperf3 -s

# На клиенте
iperf3 -c server-ip
```

**Network performance tuning:**
- TCP window size
- Buffer tuning
- Kernel network parameters (sysctl)

```bash
# Пример оптимизации
sysctl -w net.core.rmem_max=16777216
sysctl -w net.core.wmem_max=16777216
sysctl -w net.ipv4.tcp_rmem="4096 87380 16777216"
```

#### QoS (Quality of Service)
- Traffic shaping
- Prioritization
- Bandwidth management

#### IPv6 (продвинутый уровень)
- Dual-stack networks
- Tunneling (6to4, Teredo)
- IPv6 routing
- Migration стратегии

#### Network Automation
- Ansible для сетевого оборудования
- Python + Netmiko/NAPALM
- Network CI/CD
- Связано с: [[Ansible]], [[Python для SysAdmin]]

#### Мониторинг сетей
- SNMP (Simple Network Management Protocol)
- NetFlow/sFlow
- Связано с: [[Системы мониторинга]]
- Network visibility tools

## 🔗 Связанные темы

### Обязательно изучить вместе
- [[DNS]] и [[DHCP]] - основные сетевые службы
- [[SSH и удаленный доступ]] - безопасное подключение
- [[Firewall и сетевая безопасность]] - защита сетей

### Применяется в
- [[Docker]] - понимание Docker сетей
- [[Kubernetes]] - сетевые политики и CNI
- [[Load Balancing]] - балансировка трафика
- [[Nginx]] - reverse proxy и load balancer

### Сетевое оборудование
- [[Сетевое оборудование]] - роутеры, свичи, firewall

## 📚 Ресурсы для изучения

### Книги
- "Computer Networking: A Top-Down Approach" - отличный учебник
- "TCP/IP Illustrated" by W. Richard Stevens - классика
- "Network Warrior" - практический подход

### Онлайн курсы
- CCNA (Cisco) - хороший фундамент
- CompTIA Network+
- Курсы на Coursera по сетям

### Практика
- GNS3 - симулятор сетей
- Packet Tracer (Cisco)
- EVE-NG - эмулятор сетей

### Инструменты
- Wireshark - анализ пакетов
- nmap - сканирование сетей
- Netcat - Swiss Army Knife для сетей

## 💡 Практические советы

### Для Junior
1. **Практикуйся с командами** ежедневно
2. **Пинги все** - понимай топологию
3. **Читай логи** - они многое говорят
4. **Рисуй диаграммы** - визуализация помогает
5. **Используй traceroute** для понимания маршрутов

### Для Middle
1. **Изучай tcpdump** - бесценный навык
2. **Настраивай VPN** - домашняя лаборатория
3. **Понимай Wireshark** - глубокий анализ
4. **Практикуй subnetting** - должно быть автоматически
5. **Изучи firewall правила** - безопасность важна

### Для Senior
1. **Проектируй с нуля** - архитектура сетей
2. **Автоматизируй** - сетевая автоматизация
3. **Оптимизируй производительность**
4. **Внедряй monitoring** - visibility критична
5. **Планируй disaster recovery**

## 🎯 Практические задачи

### Junior
- [ ] Настроить домашнюю сеть с правильным subnetting
- [ ] Понять разницу между TCP и UDP на практике
- [ ] Использовать ping, traceroute, nslookup
- [ ] Настроить статический IP на [[Linux]] машине
- [ ] Понять как работает DHCP и DNS

### Middle
- [ ] Настроить OpenVPN сервер
- [ ] Настроить iptables firewall
- [ ] Провести анализ трафика с tcpdump
- [ ] Настроить VLAN в домашней лаборатории
- [ ] Реализовать port forwarding

### Senior
- [ ] Спроектировать enterprise сеть с нуля
- [ ] Настроить BGP или OSPF
- [ ] Реализовать network automation
- [ ] Провести full network audit
- [ ] Оптимизировать производительность сети

## 🚨 Типичные проблемы и решения

### "Сеть не работает"
1. Проверь физическое подключение
2. Проверь IP конфигурацию (`ip addr`)
3. Проверь default gateway (`ip route`)
4. Проверь DNS (`nslookup`)
5. Проверь firewall правила

### "Медленная сеть"
1. Измерь пропускную способность (iperf)
2. Проверь latency (ping, mtr)
3. Анализируй трафик (tcpdump)
4. Проверь загрузку оборудования
5. Ищи packet loss

### "Не резолвится DNS"
1. Проверь `/etc/resolv.conf`
2. Проверь доступность DNS сервера
3. Используй nslookup/dig для диагностики
4. Проверь firewall (порт 53 UDP)

## 📈 Метрики для мониторинга

- **Bandwidth utilization** - использование полосы
- **Latency** - задержка (ping time)
- **Packet loss** - потери пакетов
- **Jitter** - вариация задержки
- **Throughput** - пропускная способность
- **Connection count** - количество соединений
- **Error rate** - количество ошибок

---

**Связанные уровни:**
- 🎓 [[Junior SysAdmin]] - базовые сети (ОБЯЗАТЕЛЬНО)
- 📈 [[Middle SysAdmin]] - продвинутые сети
- 🚀 [[Senior SysAdmin]] - экспертный уровень

**Критически важно для:**
- Всех уровней системного администратора
- [[Docker]], [[Kubernetes]] - понимание контейнерных сетей
- [[Troubleshooting]] - большинство проблем связаны с сетями

#технология #сети #networking #tcp-ip #базовый-навык #критический-навык
