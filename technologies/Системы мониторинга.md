# Системы мониторинга

> Инструменты для наблюдения за состоянием инфраструктуры и приложений

## 🎯 Почему это критически важно

"You can't improve what you don't measure" - без мониторинга ты не знаешь что происходит с твоей инфраструктурой. Мониторинг - это глаза и уши системного администратора, позволяющие обнаруживать проблемы до того, как они повлияют на пользователей.

## 📋 Необходимые знания перед изучением

**Базовые знания:**
- [[Linux]] - понимание системных метрик
- [[Основы сетей]] - для мониторинга сетевых сервисов
- [[Bash]] - для написания кастомных проверок

**Полезно знать:**
- [[Docker]] - для запуска систем мониторинга
- [[Python для SysAdmin]] - для кастомных скриптов сбора метрик

## 📊 Основные концепции

### Типы мониторинга
1. **Infrastructure monitoring** - серверы, сеть, диски
2. **Application monitoring** - производительность приложений
3. **Log monitoring** - анализ логов (см. [[Логирование]])
4. **Synthetic monitoring** - проверка доступности
5. **Real User Monitoring (RUM)** - мониторинг со стороны пользователей

### Ключевые метрики (Golden Signals)
1. **Latency** - время отклика
2. **Traffic** - количество запросов
3. **Errors** - частота ошибок
4. **Saturation** - загрузка ресурсов

### USE метод (для инфраструктуры)
- **Utilization** - процент использования ресурса
- **Saturation** - насколько ресурс перегружен
- **Errors** - количество ошибок

## 🔧 Популярные системы мониторинга

### Prometheus + Grafana (современный стандарт)

#### Prometheus
**Архитектура:**
- Pull-based модель
- Time-series database
- PromQL - язык запросов
- Service discovery
- Alertmanager для оповещений

**Установка (Docker):**
```yaml
# docker-compose.yml
version: '3'
services:
  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
volumes:
  prometheus_data:
```

**Конфигурация prometheus.yml:**
```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  - job_name: 'node_exporter'
    static_configs:
      - targets: 
        - 'node1:9100'
        - 'node2:9100'
  
  - job_name: 'docker'
    static_configs:
      - targets: ['cadvisor:8080']
```

**Exporters (агенты сбора метрик):**
- **node_exporter** - метрики системы (CPU, память, диск)
- **cadvisor** - метрики контейнеров
- **mysqld_exporter** - метрики MySQL
- **postgres_exporter** - метрики PostgreSQL
- **nginx_exporter** - метрики Nginx
- **blackbox_exporter** - проверка доступности HTTP/ICMP

**Установка node_exporter:**
```bash
# На мониторируемом сервере
wget https://github.com/prometheus/node_exporter/releases/download/v1.3.1/node_exporter-1.3.1.linux-amd64.tar.gz
tar xvfz node_exporter-1.3.1.linux-amd64.tar.gz
cd node_exporter-1.3.1.linux-amd64
./node_exporter

# Или через systemd
# /etc/systemd/system/node_exporter.service
```

**PromQL примеры:**
```promql
# CPU использование
100 - (avg by (instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)

# Свободная память
node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes * 100

# Использование диска
100 - ((node_filesystem_avail_bytes / node_filesystem_size_bytes) * 100)

# HTTP ошибки rate
rate(http_requests_total{status=~"5.."}[5m])

# Top 5 endpoints по latency
topk(5, histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])))
```

#### Grafana
**Визуализация метрик:**
```yaml
# docker-compose.yml (добавляем к Prometheus)
  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
```

**Настройка:**
1. Добавить Prometheus как data source
2. Импортировать готовые дашборды (например, Node Exporter Full - ID 1860)
3. Создать свои дашборды

**Типы визуализаций:**
- Time series - графики во времени
- Gauge - текущее значение
- Stat - одиночная метрика
- Table - табличное представление
- Heatmap - тепловая карта

### Zabbix (enterprise решение)

**Особенности:**
- Агенты на всех хостах
- Web интерфейс
- Автоматическое обнаружение
- Templates для различных сервисов
- Мощный alerting

**Установка агента:**
```bash
# Ubuntu
apt install zabbix-agent

# Конфигурация /etc/zabbix/zabbix_agentd.conf
Server=zabbix-server-ip
ServerActive=zabbix-server-ip
Hostname=web01

systemctl restart zabbix-agent
```

**Что мониторить:**
- Системные метрики (CPU, RAM, disk, network)
- Сервисы (Apache, MySQL, PostgreSQL)
- Лог файлы
- Пользовательские метрики через UserParameter

### Nagios/Icinga (классические решения)

**Особенности:**
- Check-based мониторинг
- Плагины для всего
- NRPE для удаленных проверок
- Icinga - современный форк Nagios

**Типичные проверки:**
```bash
# Check HTTP
check_http -H example.com

# Check SSH
check_ssh -H server.example.com

# Check disk space
check_disk -w 20% -c 10% -p /

# Check load average
check_load -w 5,4,3 -c 10,8,6
```

### ELK Stack для мониторинга логов
См. [[Логирование]] для детальной информации.

## 📈 Уровни владения

### [[Middle SysAdmin]] уровень

**Базовое понимание:**
- Установка и настройка системы мониторинга
- Настройка базовых метрик (CPU, RAM, disk, network)
- Создание простых дашбордов
- Настройка базовых алертов
- Реакция на alerts

**Практические задачи:**
```yaml
# Prometheus alert rules
groups:
  - name: instance_alerts
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "Instance {{ $labels.instance }} down"
          description: "{{ $labels.instance }} has been down for more than 5 minutes."
      
      - alert: HighCpuUsage
        expr: 100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 80
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "High CPU on {{ $labels.instance }}"
```

**Integration с другими системами:**
- Связано с: [[Метрики и алертинг]]
- Email, Slack, PagerDuty для оповещений
- Webhook интеграции

### [[Senior SysAdmin]] уровень

**Продвинутое использование:**

**1. Observability (не просто мониторинг)**
- Metrics + Logs + Traces
- Distributed tracing (Jaeger, Zipkin)
- Связано с: [[Kubernetes]] для microservices

**2. SLO/SLI/SLA**
```
SLI (Service Level Indicator) - метрика
  → Доступность сервиса = uptime / total_time

SLO (Service Level Objective) - цель
  → Доступность должна быть ≥ 99.9%

SLA (Service Level Agreement) - контракт
  → Если доступность < 99.9%, возврат денег
```

**Error budgets:**
```
Error budget = 100% - SLO
Если SLO = 99.9%, то error budget = 0.1%

На месяц (30 дней):
Downtime budget = 30 * 24 * 60 * 0.001 = 43.2 минуты
```

**3. Capacity Planning**
- Прогнозирование роста метрик
- Планирование масштабирования
- Связано с: [[Capacity Planning]]

**4. Автоматизация реакции на alerts**
```python
# Пример автоматического масштабирования
import requests

def scale_up(service_name):
    # При высокой нагрузке - добавить инстансы
    if cpu_usage > 80 and duration > 10:
        kubernetes_api.scale_deployment(service_name, replicas=current+2)
```

**5. Advanced Grafana**
- Variables в дашбордах
- Templating
- Annotations
- Alerts в Grafana
- Плагины

**Пример переменной:**
```
Variable: $instance
Query: label_values(node_cpu_seconds_total, instance)

Затем в запросах:
node_memory_MemAvailable_bytes{instance="$instance"}
```

**6. Custom Exporters**
```python
# Пример простого Prometheus exporter
from prometheus_client import start_http_server, Gauge
import time

# Создаем метрику
custom_metric = Gauge('my_custom_metric', 'Description of metric')

def collect_metrics():
    while True:
        # Собираем данные
        value = get_some_value()
        custom_metric.set(value)
        time.sleep(60)

if __name__ == '__main__':
    start_http_server(8000)
    collect_metrics()
```

**7. Multi-cluster monitoring**
- Federation в Prometheus
- Thanos для long-term storage
- Cortex для multi-tenancy

**8. Anomaly Detection**
- Machine Learning для обнаружения аномалий
- Baseline modeling
- Predictive alerts

## 🔗 Связанные технологии

### Необходимы для работы
- [[Linux]] - системные метрики
- [[Docker]] - часто используется для запуска мониторинга
- [[Основы сетей]] - для мониторинга сетевых сервисов

### Тесно связанные
- [[Логирование]] - logs часть observability
- [[Метрики и алертинг]] - настройка оповещений
- [[Kubernetes]] - мониторинг K8s кластеров

### Дополняющие
- [[CI-CD]] - мониторинг пайплайнов
- [[SQL базы данных]] - мониторинг БД
- [[Nginx]] - мониторинг веб-серверов

## 💡 Best Practices

### 1. Что мониторить (USE/RED методы)

**Для инфраструктуры (USE):**
- CPU: utilization, saturation (load), errors
- Memory: utilization, saturation (swap), errors
- Disk: utilization (space), saturation (IOPS), errors (bad blocks)
- Network: utilization (bandwidth), saturation (queue), errors (drops)

**Для приложений (RED):**
- **R**ate - количество запросов в секунду
- **E**rrors - частота ошибок
- **D**uration - время отклика

### 2. Alerting best practices
- **Не спамь alerts** - только actionable alerts
- **Правильная severity**: critical, warning, info
- **Включай контекст** - что, где, когда, runbook
- **Группируй alerts** - не 100 одинаковых алертов
- **Используй inhibition** - если сервер down, не слать alerts о сервисах на нем

### 3. Dashboard design
- **Одна цель = один дашборд** (overview, troubleshooting, debugging)
- **Размещай важное сверху**
- **Используй цвета осмысленно** (красный = плохо)
- **Добавляй описания** к панелям
- **Time range селектор** - всегда доступен

### 4. Data retention
```
Raw metrics: 15 days (full resolution)
5m aggregates: 30 days
1h aggregates: 1 year
```

## 🎯 Практические задачи

### Middle уровень
- [ ] Установить Prometheus + Grafana + node_exporter
- [ ] Настроить мониторинг 3+ серверов
- [ ] Создать дашборд для системных метрик
- [ ] Настроить alerts для critical метрик
- [ ] Настроить оповещения в Slack/Email
- [ ] Мониторить веб-сервер и БД

### Senior уровень
- [ ] Внедрить full observability (metrics + logs + traces)
- [ ] Настроить SLO/SLI мониторинг
- [ ] Создать custom exporter
- [ ] Внедрить capacity planning дашборды
- [ ] Настроить Thanos/Cortex для long-term storage
- [ ] Реализовать автоматическую реакцию на alerts
- [ ] Мониторинг K8s кластера
- [ ] Создать runbooks для каждого alert

## 🚨 Типичные ошибки

1. **Alert fatigue** - слишком много ненужных alerts
2. **Не тестировать alerts** - не проверять что alerts работают
3. **Мониторить всё подряд** - фокусируйся на важном
4. **Не документировать alerts** - добавляй runbooks
5. **Игнорировать retention** - бесконечное хранение = дорого
6. **Не мониторить сам мониторинг** - who watches the watchmen?
7. **Не использовать labels правильно** - усложняет запросы

## 📚 Ресурсы для изучения

### Книги
- "Site Reliability Engineering" от Google
- "Prometheus: Up & Running"
- "The Art of Monitoring"

### Онлайн ресурсы
- Prometheus документация - отличная
- Grafana tutorials
- PromQL for Humans

### Практика
- Play with Prometheus - онлайн песочница
- PromLabs - интерактивные уроки PromQL

## 📈 Метрики успеха мониторинга

- **MTTD** (Mean Time To Detect) - как быстро обнаруживаем проблему
- **MTTR** (Mean Time To Resolve) - как быстро решаем
- **Alert precision** - % полезных alerts (не false positives)
- **Coverage** - % системы под мониторингом

---

**Связанные уровни:**
- 📈 [[Middle SysAdmin]] - базовый мониторинг (ОБЯЗАТЕЛЬНО)
- 🚀 [[Senior SysAdmin]] - продвинутый мониторинг и observability

**Требует знания:**
- [[Linux]] - обязательно
- [[Docker]] - полезно
- [[Kubernetes]] - для K8s мониторинга

#технология #мониторинг #prometheus #grafana #observability #sre
