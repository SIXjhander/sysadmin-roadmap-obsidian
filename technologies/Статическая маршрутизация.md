# Статическая маршрутизация

> Ручная настройка маршрутов между сетями

## 🎯 Почему это важно

Статическая маршрутизация - это фундаментальный навык для понимания работы сетей. Хотя в крупных сетях используется динамическая маршрутизация, статические маршруты необходимы для:
- Небольших сетей и офисов
- Резервных маршрутов
- Специальных случаев маршрутизации
- Понимания основ работы IP-маршрутизации

## 📋 Необходимые знания перед изучением

**Обязательно нужно знать:**
- [[technologies/Основы сетей]] - понимание IP-адресации, подсетей, default gateway
- [[DNS]] - базовое понимание сетевых служб

**Полезно знать:**
- [[technologies/Linux]] или [[technologies/Windows Server]] - для практики
- [[technologies/Сетевое оборудование]] - для настройки на роутерах

## 📊 Уровни владения

### [[levels/Junior SysAdmin]] уровень

#### Основы статической маршрутизации

**Что такое маршрутизация:**
- Процесс пересылки пакетов между сетями
- Таблица маршрутизации (routing table)
- Default gateway (шлюз по умолчанию)
- Metric (метрика маршрута)

**Просмотр таблицы маршрутизации:**

Linux:
```bash
# Просмотр маршрутов
ip route show
# или
route -n

# Пример вывода:
# default via 192.168.1.1 dev eth0
# 192.168.1.0/24 dev eth0 proto kernel scope link src 192.168.1.100
```

Windows:
```powershell
# Просмотр маршрутов
route print
# или
Get-NetRoute

# Просмотр только IPv4 маршрутов
route print -4
```

**Добавление простого статического маршрута:**

Linux:
```bash
# Добавить маршрут к сети 10.0.2.0/24 через gateway 192.168.1.254
ip route add 10.0.2.0/24 via 192.168.1.254

# Или старым способом
route add -net 10.0.2.0 netmask 255.255.255.0 gw 192.168.1.254
```

Windows:
```powershell
# Добавить маршрут
route add 10.0.2.0 mask 255.255.255.0 192.168.1.254

# Или через PowerShell
New-NetRoute -DestinationPrefix "10.0.2.0/24" -NextHop "192.168.1.254"
```

**Удаление маршрута:**

Linux:
```bash
# Удалить маршрут
ip route del 10.0.2.0/24

# Старый способ
route del -net 10.0.2.0 netmask 255.255.255.0
```

Windows:
```powershell
# Удалить маршрут
route delete 10.0.2.0

# PowerShell
Remove-NetRoute -DestinationPrefix "10.0.2.0/24"
```

**Основные концепции:**
- **Destination** - целевая сеть
- **Gateway/NextHop** - следующий хоп (IP шлюза)
- **Interface** - исходящий интерфейс
- **Metric** - приоритет маршрута (меньше = выше приоритет)

### [[levels/Middle SysAdmin]] уровень

#### Продвинутая настройка статических маршрутов

**Persistent маршруты (сохраняются после перезагрузки):**

Linux (различные методы):
```bash
# CentOS/RHEL - в файле /etc/sysconfig/network-scripts/route-eth0
10.0.2.0/24 via 192.168.1.254

# Ubuntu/Debian - в /etc/network/interfaces
up ip route add 10.0.2.0/24 via 192.168.1.254

# Или в /etc/netplan/*.yaml
routes:
  - to: 10.0.2.0/24
    via: 192.168.1.254
    metric: 100

# Современный способ - NetworkManager
nmcli connection modify eth0 +ipv4.routes "10.0.2.0/24 192.168.1.254"
```

Windows:
```powershell
# Флаг -p делает маршрут постоянным
route add 10.0.2.0 mask 255.255.255.0 192.168.1.254 -p

# PowerShell с сохранением
New-NetRoute -DestinationPrefix "10.0.2.0/24" -NextHop "192.168.1.254" -PolicyStore PersistentStore
```

**Маршруты с метрикой:**
```bash
# Linux - метрика 100
ip route add 10.0.2.0/24 via 192.168.1.254 metric 100

# Windows
route add 10.0.2.0 mask 255.255.255.0 192.168.1.254 metric 100
```

**Маршрут через конкретный интерфейс:**
```bash
# Linux
ip route add 10.0.2.0/24 via 192.168.1.254 dev eth0

# Windows
route add 10.0.2.0 mask 255.255.255.0 192.168.1.254 if 12
# где 12 - индекс интерфейса из route print
```

**Multiple маршруты (балансировка):**
```bash
# Linux - load balancing между двумя путями
ip route add 10.0.2.0/24 \
  nexthop via 192.168.1.254 weight 1 \
  nexthop via 192.168.1.253 weight 1
```

**Null route (black hole):**
```bash
# Linux - отбросить пакеты для сети
ip route add blackhole 10.0.99.0/24

# Windows - маршрут "в никуда"
route add 10.0.99.0 mask 255.255.255.0 0.0.0.0
```

**Troubleshooting маршрутизации:**
```bash
# Проверка какой маршрут используется
# Linux
ip route get 10.0.2.50

# Windows
Test-NetConnection -ComputerName 10.0.2.50 -DiagnoseRouting

# Трассировка маршрута
traceroute 10.0.2.50  # Linux
tracert 10.0.2.50     # Windows
```

**Дополнительные навыки:**
- Policy-based routing (продвинутая маршрутизация по политикам)
- Route aggregation (агрегация маршрутов)
- Понимание работы ARP с маршрутизацией
- Работа с VLAN и маршрутизацией между ними

### [[levels/Senior SysAdmin]] уровень

#### Enterprise-уровень статической маршрутизации

**Policy-based routing (PBR):**
```bash
# Linux - маршрутизация на основе source IP
# Создать отдельную таблицу маршрутизации
echo "100 admin_table" >> /etc/iproute2/rt_tables

# Добавить маршрут в эту таблицу
ip route add default via 192.168.1.254 table admin_table

# Правило: трафик от 192.168.100.0/24 использует эту таблицу
ip rule add from 192.168.100.0/24 table admin_table
```

**Failover маршрутизация:**
```bash
# Linux - primary и backup маршруты
# Primary маршрут с метрикой 10
ip route add 10.0.2.0/24 via 192.168.1.254 metric 10

# Backup маршрут с метрикой 20
ip route add 10.0.2.0/24 via 192.168.1.253 metric 20

# Если primary недоступен, автоматически переключится на backup
```

**Asymmetric routing handling:**
```bash
# Отключить reverse path filtering для asymmetric routing
echo 0 > /proc/sys/net/ipv4/conf/all/rp_filter
echo 0 > /proc/sys/net/ipv4/conf/eth0/rp_filter

# Или через sysctl
sysctl -w net.ipv4.conf.all.rp_filter=0
```

**Route summarization (агрегация):**
```
# Вместо:
# 192.168.1.0/24
# 192.168.2.0/24
# 192.168.3.0/24
# 192.168.4.0/24

# Используем суммарный маршрут:
ip route add 192.168.0.0/22 via 10.0.0.1
```

**Мониторинг и автоматизация:**
```bash
# Скрипт для проверки доступности gateway и переключения маршрутов
#!/bin/bash
PRIMARY_GW="192.168.1.254"
BACKUP_GW="192.168.1.253"
DEST_NET="10.0.2.0/24"

while true; do
    if ping -c 1 -W 1 $PRIMARY_GW > /dev/null 2>&1; then
        # Primary доступен, убедимся что используется он
        if ! ip route show | grep -q "$DEST_NET via $PRIMARY_GW"; then
            ip route del $DEST_NET
            ip route add $DEST_NET via $PRIMARY_GW metric 10
            ip route add $DEST_NET via $BACKUP_GW metric 20
        fi
    else
        # Primary недоступен, используем backup
        echo "Primary gateway down, using backup"
    fi
    sleep 5
done
```

**Экспертные навыки:**
- Интеграция статических маршрутов с [[technologies/BGP]] и [[technologies/OSPF]]
- Route redistribution между статическими и динамическими протоколами
- VRF (Virtual Routing and Forwarding) для изоляции маршрутов
- Связано с: [[technologies/VLAN]], [[technologies/VPN]]

## 🔗 Связанные технологии

### Обязательные зависимости (нужны перед изучением)
- [[technologies/Основы сетей]] - без понимания IP-адресации и подсетей невозможно работать с маршрутизацией

### Тесно связанные темы
- [[technologies/Маршрутизация]] - общая тема маршрутизации
- [[technologies/Сетевое оборудование]] - настройка маршрутов на роутерах
- [[technologies/VLAN]] - маршрутизация между VLAN
- [[technologies/VPN]] - маршруты для VPN туннелей

### Применяется в
- [[technologies/Linux]] - маршрутизация на Linux серверах
- [[technologies/Windows Server]] - маршрутизация на Windows
- [[technologies/Firewall и сетевая безопасность]] - маршруты через firewall

### Альтернативы
- **[[technologies/OSPF]]** - для средних и крупных сетей с динамической топологией
- **[[technologies/BGP]]** - для больших сетей и интернет-маршрутизации
- **RIP** (устарел) - старый протокол динамической маршрутизации

## 📚 Ресурсы для изучения

### Официальная документация
- [iproute2 documentation](https://wiki.linuxfoundation.org/networking/iproute2) - документация по ip route
- [Microsoft routing documentation](https://docs.microsoft.com/en-us/windows-server/networking/technologies/routing) - Windows routing

### Книги
- "TCP/IP Routing Technologies" by Jeff Doyle - подробно о маршрутизации
- "Computer Networking: A Top-Down Approach" - основы сетей и маршрутизации

### Онлайн курсы
- [Cisco CCNA] Routing and Switching - хороший фундамент
- [Linux Foundation] Linux Networking курсы

### Практика
- GNS3 - симулятор сетей для практики
- Packet Tracer - Cisco симулятор
- Домашняя лаборатория с виртуальными машинами

## 💡 Best Practices

### Основные принципы
1. **Документируй все маршруты** - создай таблицу с описанием каждого маршрута и его назначения
2. **Используй метрики осознанно** - меньшая метрика = выше приоритет
3. **Планируй redundancy** - настраивай backup маршруты где это критично
4. **Тестируй перед применением** - проверяй маршруты в тестовой среде
5. **Делай маршруты persistent** - чтобы они сохранялись после перезагрузки

### Безопасность
1. **Ограничивай административный доступ** - только авторизованные лица могут менять маршруты
2. **Логируй изменения** - все изменения маршрутов должны логироваться
3. **Используй null routes** для блокировки нежелательного трафика
4. **Проверяй source** - используй reverse path filtering где возможно

### Производительность
1. **Минимизируй количество маршрутов** - используй агрегацию где возможно
2. **Правильная метрика** - направляй трафик по оптимальным путям
3. **Избегай routing loops** - тщательно планируй топологию
4. **Мониторь латентность** - отслеживай производительность маршрутов

## 🎯 Практические задачи

### Junior уровень
- [ ] Просмотреть текущую таблицу маршрутизации на своем компьютере
- [ ] Добавить временный статический маршрут к тестовой сети
- [ ] Удалить созданный маршрут
- [ ] Использовать traceroute для просмотра пути пакетов
- [ ] Понять что такое default gateway и как он работает

### Middle уровень
- [ ] Настроить persistent статический маршрут на Linux сервере
- [ ] Настроить persistent статический маршрут на Windows Server
- [ ] Создать два маршрута к одной сети с разными метриками (primary/backup)
- [ ] Настроить маршрутизацию между двумя VLAN
- [ ] Написать скрипт для автоматического добавления маршрутов
- [ ] Использовать null route для блокировки доступа к подсети
- [ ] Провести troubleshooting проблемы с маршрутизацией используя ip route get и traceroute

### Senior уровень
- [ ] Реализовать policy-based routing для разделения трафика
- [ ] Настроить автоматический failover между двумя gateway
- [ ] Спроектировать схему маршрутизации для multi-site сети
- [ ] Реализовать route aggregation для оптимизации таблицы маршрутизации
- [ ] Настроить asymmetric routing handling
- [ ] Создать систему мониторинга состояния маршрутов
- [ ] Интегрировать статические маршруты с динамической маршрутизацией

## 🚨 Типичные ошибки

1. **Забыть сделать маршрут persistent** - после перезагрузки маршрут исчезает и сеть не работает
2. **Неправильная маска подсети** - маршрут не работает или работает не для той сети
3. **Gateway не в той подсети** - gateway должен быть в локальной подсети интерфейса
4. **Не проверить connectivity к gateway** - если gateway недоступен, маршрут не работает
5. **Routing loops** - два роутера отправляют пакеты друг другу бесконечно
6. **Конфликтующие маршруты** - два маршрута с одинаковой метрикой к одной сети
7. **Забыть про reverse traffic** - настроить маршрут только в одну сторону
8. **Не документировать** - через месяц не помнишь зачем этот маршрут нужен
9. **Одинаковые метрики** - непредсказуемое поведение при наличии нескольких путей
10. **Не тестировать в нерабочее время** - проблема обнаруживается когда все работают

## 🔧 Troubleshooting

| Проблема | Возможная причина | Решение |
|----------|-------------------|---------|
| Маршрут не работает | Gateway недоступен | Проверь: `ping <gateway_ip>` |
| Маршрут исчез после reboot | Не сделан persistent | Добавь в конфигурацию системы |
| Пакеты не доходят | Неправильная маска/сеть | Проверь: `ip route get <dest_ip>` |
| Медленная работа | Неоптимальный путь | Проверь метрики и traceroute |
| Асимметричная маршрутизация | Разные пути туда и обратно | Отключи rp_filter или исправь маршруты |
| Conflicting routes error | Маршрут уже существует | Удали старый: `ip route del ...` |
| Network unreachable | Нет маршрута к gateway | Добавь маршрут к gateway сети |
| Cannot assign requested address | Gateway не в локальной сети | Исправь IP gateway |

## 📈 Метрики успеха

- **Понимание таблицы маршрутизации** - можешь объяснить каждую строку в route table
- **Troubleshooting** - можешь быстро найти проблему с маршрутизацией используя ip route get и traceroute
- **Планирование** - можешь спроектировать схему маршрутизации для новой сети
- **Автоматизация** - написал скрипты для управления маршрутами
- **Best practices** - все маршруты документированы, persistent, протестированы

---

**Связанные уровни:**
- 🎓 [[levels/Junior SysAdmin]] - базовое понимание и просмотр маршрутов
- 📈 [[levels/Middle SysAdmin]] - настройка persistent маршрутов, troubleshooting
- 🚀 [[levels/Senior SysAdmin]] - policy-based routing, failover, enterprise решения

**Категория:** #сети #маршрутизация #networking

---

**Дата создания:** 2026-01-12
**Последнее обновление:** 2026-01-12
**Автор:** SysAdmin Roadmap Contributors
