# Docker

> Платформа для контейнеризации приложений

## 🎯 Почему это важно

Docker революционизировал способ разработки, доставки и запуска приложений. Контейнеры обеспечивают изоляцию, портативность и эффективность, что делает Docker незаменимым инструментом современного системного администратора.

## 📋 Необходимые знания перед изучением

**Обязательно нужно знать:**
- [[Linux]] - основы работы в терминале, файловая система, процессы
- [[Основы сетей]] - понимание сетевых интерфейсов, портов, NAT

**Полезно знать:**
- [[Bash]] - для написания Dockerfile и скриптов
- [[Git]] - для версионирования конфигураций

## 📊 Уровни владения

### [[Middle SysAdmin]] уровень (начальный Docker)

#### Основы Docker
- **Базовые концепции**
  - Контейнеры vs виртуальные машины
  - Images vs Containers
  - Docker Engine архитектура
  - Docker Hub и registry

- **Работа с контейнерами**
  ```bash
  # Основные команды
  docker run                    # Запуск контейнера
  docker ps                     # Список запущенных контейнеров
  docker ps -a                  # Все контейнеры
  docker stop/start/restart     # Управление контейнерами
  docker rm                     # Удаление контейнера
  docker logs                   # Просмотр логов
  docker exec                   # Выполнение команд в контейнере
  ```

- **Работа с образами**
  ```bash
  docker images                 # Список образов
  docker pull                   # Скачать образ
  docker build                  # Собрать образ
  docker push                   # Загрузить в registry
  docker rmi                    # Удалить образ
  docker tag                    # Тегирование образов
  ```

- **Dockerfile**
  ```dockerfile
  FROM ubuntu:22.04
  RUN apt-get update && apt-get install -y nginx
  COPY ./app /var/www/html
  EXPOSE 80
  CMD ["nginx", "-g", "daemon off;"]
  ```
  - Основные инструкции: FROM, RUN, COPY, ADD, WORKDIR, EXPOSE, CMD, ENTRYPOINT
  - Multi-stage builds (базовое понимание)
  - Best practices для Dockerfile

- **Docker Compose**
  ```yaml
  version: '3.8'
  services:
    web:
      build: .
      ports:
        - "80:80"
    db:
      image: postgres:14
      environment:
        POSTGRES_PASSWORD: secret
  ```
  - Определение многоконтейнерных приложений
  - Основные команды: `docker-compose up/down/logs`
  - Networks и volumes в compose

- **Volumes (хранение данных)**
  - Named volumes
  - Bind mounts
  - Персистентность данных
  - Backup и restore volumes

- **Сети**
  - Bridge network (по умолчанию)
  - Host network
  - Custom networks
  - Container-to-container коммуникация
  - Требует хорошего понимания: [[Основы сетей]]

### [[Senior SysAdmin]] уровень (продвинутый Docker)

#### Продакшн-использование
- **Безопасность**
  - Не запускать как root
  - Security scanning образов
  - Secrets management
  - AppArmor/SELinux profiles
  - Read-only file systems
  - Связано с: [[Основы информационной безопасности]]

- **Оптимизация образов**
  - Минимизация размера образов
  - Multi-stage builds (продвинутое использование)
  - Layer caching стратегии
  - Использование Alpine Linux
  - .dockerignore

- **Мониторинг и логирование**
  - Container metrics
  - Logging drivers
  - Интеграция с [[Системы мониторинга]]
  - Health checks
  - Связано с: [[Метрики и алертинг]], [[Логирование]]

- **Docker в продакшене**
  - Resource limits (CPU, Memory)
  - Restart policies
  - High availability strategies
  - Rolling updates
  - Связано с: [[Kubernetes]] для оркестрации

- **Registry management**
  - Собственный Docker Registry
  - Harbor для enterprise
  - Image signing и verification
  - Retention policies

- **Продвинутые сети**
  - Overlay networks
  - Macvlan
  - Network troubleshooting
  - Service discovery

## 🔗 Связанные технологии

### Что изучать после Docker
- [[Kubernetes]] - оркестрация контейнеров (обязательно для Middle/Senior)
- [[CI-CD]] - автоматизация сборки и деплоя Docker образов
- [[Ansible]] - автоматизация развертывания Docker

### Что использовать вместе с Docker
- [[Nginx]] - как reverse proxy для контейнеров
- [[Системы мониторинга]] - мониторинг контейнеров
- [[Git]] - версионирование Dockerfile и compose файлов

### Альтернативы и дополнения
- Podman - альтернатива Docker (daemonless)
- containerd - низкоуровневый runtime
- Docker Swarm - встроенная оркестрация (менее популярна чем K8s)

## 📚 Ресурсы для изучения

### Официальная документация
- docs.docker.com - лучший источник
- Docker Hub - репозиторий образов

### Книги
- "Docker Deep Dive" by Nigel Poulton
- "Docker in Action" by Jeff Nickoloff
- "Docker: Up & Running" by Karl Matthias

### Курсы
- Docker Mastery (Udemy)
- Play with Docker (бесплатная онлайн лаборатория)

### Практика
- Docker Playground - онлайн песочница
- Создавай свои приложения в Docker

## 💡 Best Practices

### Dockerfile
```dockerfile
# Хорошо: Multi-stage build
FROM node:18 AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
USER node
CMD ["node", "dist/index.js"]
```

### Docker Compose для разработки
```yaml
version: '3.8'
services:
  app:
    build: .
    volumes:
      - .:/app  # Bind mount для hot reload
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    depends_on:
      - db
  db:
    image: postgres:14
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=devpassword

volumes:
  postgres_data:
```

### Основные принципы
1. **Один процесс - один контейнер**
2. **Immutable infrastructure** - не изменяй running контейнеры
3. **Используй official images** как базу
4. **Минимизируй количество layers**
5. **Не храни secrets в образах**
6. **Используй .dockerignore**
7. **Тегируй образы осмысленно** (не только latest)

## 🎯 Практические задачи

### Начальный уровень (Middle)
- [ ] Установить Docker на локальную машину
- [ ] Запустить контейнер с Nginx
- [ ] Создать простой Dockerfile для Python/Node.js приложения
- [ ] Настроить Docker Compose для приложения с БД
- [ ] Пробросить порты и volumes
- [ ] Посмотреть логи контейнера

### Продвинутый уровень (Senior)
- [ ] Создать multi-stage Dockerfile с оптимизацией размера
- [ ] Настроить собственный Docker Registry
- [ ] Реализовать health checks для приложения
- [ ] Настроить мониторинг контейнеров
- [ ] Реализовать zero-downtime deployment
- [ ] Провести security audit образов
- [ ] Настроить автоматическую сборку в [[CI-CD]]

## 🚨 Типичные ошибки

1. **Запуск как root** - всегда используй USER в Dockerfile
2. **Latest tag в продакшене** - используй конкретные версии
3. **Игнорирование .dockerignore** - увеличивает build context
4. **Установка всего в один RUN** - усложняет отладку и кэширование
5. **Хранение данных в контейнере** - используй volumes
6. **Не чистить кэш пакетов** - увеличивает размер образа

## 🔄 Workflow примеры

### Разработка
```bash
# Локальная разработка
docker-compose up -d
docker-compose logs -f app
# Вносишь изменения в код
docker-compose restart app
```

### Деплой
```bash
# Сборка
docker build -t myapp:v1.2.3 .
docker tag myapp:v1.2.3 registry.company.com/myapp:v1.2.3

# Пуш в registry
docker push registry.company.com/myapp:v1.2.3

# На проде
docker pull registry.company.com/myapp:v1.2.3
docker stop myapp-old
docker run -d --name myapp registry.company.com/myapp:v1.2.3
```

## 📈 Метрики успеха

- Сократилось время развертывания
- Консистентность окружений (dev = staging = prod)
- Легкость rollback'ов
- Улучшенная изоляция приложений
- Эффективное использование ресурсов

---

**Связанные уровни:**
- 📈 [[Middle SysAdmin]] - начинаешь изучать Docker здесь
- 🚀 [[Senior SysAdmin]] - экспертное владение Docker

**Следующий шаг:**
- [[Kubernetes]] - для оркестрации множества контейнеров

#технология #docker #контейнеризация #devops
